{
  "name": "coa-auditing-website",
  "version": "1.0.0",
  "description": "Medical claims auditing website with contact form and database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:studio && npm run build:site",
    "build:studio": "cd studio && npm install && npx sanity build --base-path /studio",
    "build:site": "eleventy"
  },
  "keywords": [
    "medical",
    "claims",
    "auditing"
  ],
  "author": "COA Auditing",
  "license": "ISC",
  "dependencies": {
    "@sanity/client": "^6.29.1",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "nodemon": "^3.0.2"
  }
}
